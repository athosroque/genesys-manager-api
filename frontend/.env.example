# URL base do backend (API)
# Dev (vite): backend direto. Docker/produção: /api (proxy nginx, mesma origem).
# Nenhuma credencial/secret deve ficar no frontend — só esta URL pública.
VITE_API_BASE_URL=http://localhost:8000
