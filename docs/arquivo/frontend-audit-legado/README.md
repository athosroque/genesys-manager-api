# Frontend — auditoria legada (arquivo)

Componentes da UI anterior de auditoria (timeline/tokens). **Não importados**
pelo router nem por `AuditView.vue`.

Fluxo atual: `frontend/src/views/AuditView.vue`,
`components/AuditSearchBar.vue`, `components/UserChangesList.vue`,
`api/audits.js` → `getUserChanges`.
